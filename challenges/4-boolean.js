const temp = 32
const isFreezing = temp <= 32

if (isFreezing) {
  console.log('Is freezing outside')
}

const age = 65
const isChild = age <= 7
const isSenior = age >= 65

console.log(isSenior, isChild)
